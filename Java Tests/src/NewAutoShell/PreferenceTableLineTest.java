package NewAutoShell;

import static org.junit.jupiter.api.Assertions.*;

import java.util.HashMap;
import java.util.Map;

import org.junit.jupiter.api.Test;

import Commands.TestChaosCommand;
import edu.wpi.first.wpilibj.command.CommandGroup;

class PreferenceTableLineTest {


	void test() {
		//fail("Not yet implemented");
	
		
		//assertEquals(match("dsfds"), false);
		//assertEquals(match("test;dsfds."), true );
		//assertEquals(match(""), false);
		//assertEquals(match("jeff4&12,"), false);
		//assertEquals(match("test;yes."), true);
		
		//assertEquals(match("turn;87&-87."), true);
//		match("");
//		match("turn87.");
//		match("turn;87;87.");
//		match("turn;87;87,");
//		match("turn;87");

	}


	
	//public boolean match(String check) {
		//CommandGroup autoSequence = new CommandGroup();
		//Map<String, ChaosCommand> commands = new HashMap<String, ChaosCommand>();
		//commands.put("test", new TestChaosCommand());
		//PreferenceTableLine line = new PreferenceTableLine(check, commands);
		//System.out.print(line.getCommand() != null);
		//System.out.println(line.getIsParallel());
		//return line.getCommand() != null;
		
	//}
}
